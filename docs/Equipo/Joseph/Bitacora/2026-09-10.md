---
tipo: bitacora
autor: Joseph
fecha: 2026-09-10
issues: [5]
---

# 2026-09-10: Joseph

## Qué hice
- Análisis completo del proyecto: código, `ARCHITECTURE.md`, historial de git e issues de GitHub.
- Creé el vault de Obsidian en `docs/` (#5) con índice, notas de proyecto, estado, pendientes, deuda técnica, decisiones, errores, actas, bitácora, carpetas del equipo y plantillas.
- Agregué `.obsidian/` y `.trash/` al `.gitignore`.

## Hallazgos
- Todos los archivos del repo aparecen modificados en mi clon, pero es solo CRLF: [[ERR-001 Finales de linea CRLF marcan todo como modificado]].
- No hay ruta 404: [[ERR-002 Rutas inexistentes sin pagina 404]].
- 13 puntos de deuda técnica menores: [[Deuda tecnica]].

## Entorno arreglado (tarde)
- Instalé **Bun 1.4.2** con el script oficial. `bun install`, `bun run build` ✅ y `bun run --bun lint` ✅ (0 hallazgos).
  - Sin Node, oxlint necesita `--bun`. Comprobé con un archivo de prueba que el lint sí detecta errores.
  - `bun install` agrega `"configVersion": 0` al `bun.lock`: lo revertí porque es solo metadata.
- **ERR-001 resuelto**: `.gitattributes` con `eol=lf`, `core.autocrlf input` y reescritura de los 26 archivos en CRLF (el `.gitignore` se convirtió a mano para no perder el cambio del vault). `git status` ahora solo muestra cambios reales.
  - Aprendizaje: `git checkout -- .` no basta después de agregar `.gitattributes`. Hay que borrar el archivo y hacer checkout (detalle en ERR-001).

- Instalé **Node v24.21.0 LTS** (npm 11.19.0) con **nvm v0.40.7**. Con Node, `bun run lint` ya funciona sin `--bun` (0 hallazgos) y `bun run build` también pasa ✅.
  - Aprendizaje: Windows tiene su propio Node y WSL lo mete en el PATH (`/mnt/c/Program Files/nodejs`). Con nvm cargado gana el de Linux. Hay que revisar `which node` si algo falla raro.

## Siguiente
- Abrir el PR de #5 (vault + `.gitignore` + `.gitattributes`).
- Empezar #4 (/ejercicios).

## Implementación #4 (/ejercicios)

### Qué hice
- Implementé la página `/ejercicios` con listado de 3 retos tipo CTF y detalles en `/ejercicios/:slug`.
- Creé `src/features/exercises/` con:
  - `api/exercises.ts`: carga los 3 retos desde `src/content/exercises/` (`exercises.json` con los datos, un `.md` por enunciado y explicación, y el `.log` del reto 03)
  - `progress.ts`: guarda y recupera progreso del navegador (`localStorage`)
  - `lib/sha256.ts`: SHA-256 en JavaScript (fallback para navegadores sin HTTPS) para validar flags
  - `lib/checkFlag.ts`: compara flags con sus hashes
  - `labels.ts` y `types.ts`
  - Componentes en `components/`: `ExerciseCard.tsx`, `Markdown.tsx`, `Artifact.tsx`, `Hints.tsx`, `FlagForm.tsx`
  - Páginas: `src/pages/ejercicios/EjerciciosPage.tsx` (listado) y `EjercicioPage.tsx` (detalle con funciones locales `ExerciseDetail` y `NotFound`)
- 7 commits de feature en la rama:
  - Cripto: SHA-256 con fallback JS + tests de Vitest
  - Validación: `checkFlag` y progreso en `localStorage`
  - Contenido: API y reto 01 (Cripto)
  - Retos 02 y 03 (Web y Defensa)
  - Componentes detail
  - Páginas y ruta en el router

### Resultado de las pruebas
- `bun run test`: **30 pruebas pasadas** (Vitest)
  - Tests de SHA-256, checkFlag, progreso, integridad de contenido y solvers de los 3 retos
- `bun run lint`: **0 hallazgos** (oxlint)
- `bun run build`: ✓ built
  - CSS: 21.09 kB (gzip: 5.17 kB)
  - JS: 479.74 kB (gzip: 149.37 kB)

### Aprendizajes
- **Vitest + Node 24**: Node.js v24 no tiene `localStorage` global (se stubbea con `vi.stubGlobal()`); Node 24 SÍ tiene `crypto.subtle`, pero las pruebas lo stúbbean con `{}` para simular un navegador sin HTTPS (donde `crypto.subtle` no está disponible), ejercitando el fallback SHA-256 en JavaScript puro.
- **react-markdown**: la librería no renderiza HTML crudo (`dangerouslySetInnerHTML` no funciona). Para el reto 02 (Web), la flag oculta usa su propio componente React en vez de HTML.
- **`.gitignore` y `.log`**: el `.gitignore` raíz excluye todos los `*.log`. Agregué una excepción narrow para los logs descargables de los retos: `!apps/frontend/src/content/exercises/*.log` (por ejemplo, `quien-toco-la-puerta.auth.log`).

### Siguiente
- Revisión visual de los 3 retos en el navegador (8 puntos).
- Subir la rama y abrir PR hacia `main` cuando PR #11 esté mergeado.
